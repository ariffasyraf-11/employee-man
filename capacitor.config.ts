import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.employeeman',
  appName: 'Employee Man',
  webDir: 'dist/employee-man/browser'
};

export default config;
